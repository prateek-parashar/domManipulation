/* 
Working with set data structure

console.log("hello");

const idArray = ["test1", "test2", "test3", "test1"];
const id = new Set(idArray);

id.has("test1");

id.delete("test1");

console.log(id.size);
console.log(id.entries());

for (const i of id.entries()) {
    console.log(i[0]);
}

*/

/*
Destructuring of arrays

const idArray = ["test1", "test2", "test3", "test1"];

console.log(idArray.entries());

for (const [i, entry] of idArray.entries()) {
    console.log(`${i} ${entry}`);
}


const map = new Map();

map.set("tset", 1);

console.log(map.entries();


let person = { name: "Max", age: 30 };
const weak = new WeakSet();

weak.add(person);

*/
